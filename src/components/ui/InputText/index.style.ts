import { css } from "@emotion/react"

export const style = {
  inputText: css({
    display: "flex",
    color: "red",
  }),

  label: css({
    fontSize: "12px",
  }),

  input: css({
    marginLeft: "8px",
  }),
}
